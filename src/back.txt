        <!-- <h3 class="text-xl font-semibold text-blue-600 mb-4">{{ partido.local.nombre }}</h3> -->

            <!-- Subtabs -->
            <!-- <div class="flex border-b mb-4">
                <button v-for="sub in subtabs" :key="sub" @click="activeSubtabLocal = sub"
                    class="flex-1 py-2 text-center font-medium"
                    :class="activeSubtabLocal === sub ? 'border-b-2 border-blue-500 text-blue-600' : 'text-gray-500'">
                    {{ sub }}
                </button>
            </div> -->

            <!-- Titulares -->
            <!-- <div v-if="activeSubtabLocal === 'Titulares'">
                <TablaJugadores :jugadores="partido.local.titulares" />
            </div> -->

            <!-- Suplentes -->
            <!-- <div v-if="activeSubtabLocal === 'Suplentes'">
                <TablaJugadores :jugadores="partido.local.suplentes" />
            </div> -->

            <!-- CT -->
            <!-- <div v-if="activeSubtabLocal === 'Cuerpo Técnico'">
                <TableCT :ct="partido.local.ct" />
            </div> -->





               <!-- Subtabs -->
        <!-- <div class="flex border-b mb-4">
                <button v-for="sub in subtabs" :key="sub" @click="activeSubtabVisitante = sub"
                    class="flex-1 py-2 text-center font-medium"
                    :class="activeSubtabVisitante === sub ? 'border-b-2 border-green-500 text-green-600' : 'text-gray-500'">
                    {{ sub }}
                </button>
            </div> -->

        <!-- Titulares -->
        <!-- <div v-if="activeSubtabVisitante === 'Titulares'">
                <TablaJugadores :jugadores="partido.visitante.titulares" />
            </div> -->

        <!-- Suplentes -->
        <!-- <div v-if="activeSubtabVisitante === 'Suplentes'">
                <TablaJugadores :jugadores="partido.visitante.suplentes" />
            </div> -->

        <!-- CT -->
        <!-- <div v-if="activeSubtabVisitante === 'Cuerpo Técnico'">
                <TableCT :ct="partido.visitante.ct" />
            </div> -->
        <!-- </div> -->
        <!-- <div v-if="activeTab === 'Completar incidencias'" class="flex gap-4 items-center justify-between">
            <Incidencias :amonestados="amonestadosLocal" :expulsados="expulsadosLocal"
                :sustituciones="sustitucionesLocal" :club="partido.local.nombre" />
            <Incidencias :amonestados="amonestadosVisitante" :expulsados="expulsadosVisitante"
                :sustituciones="sustitucionesVisitante" :club="partido.visitante.nombre" />
        </div> -->
        <!-- INFORME -->
        <!-- <div v-if="activeTab === 'Informe'" class="space-y-4">
            <h3 class="text-xl font-semibold text-gray-700">Informe del Partido</h3>
            <textarea v-model="informe" class="w-full h-40 p-3 border rounded-lg resize-none"
                placeholder="Escribe el informe aquí..."></textarea>
        </div> -->
            <!-- <h3 class="text-xl font-semibold text-green-600 mb-4">{{ partido.visitante.nombre }}</h3> -->
